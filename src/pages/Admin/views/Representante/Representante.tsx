import { SchemeView } from '../../components'
import { RepresentanteModel } from '../../models'

const Representante = () => (
  <SchemeView metaModel={RepresentanteModel.metaModel} />
)

export default Representante
