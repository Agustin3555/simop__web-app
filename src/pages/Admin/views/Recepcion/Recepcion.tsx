import { SchemeView } from '../../components'
import { RecepcionMeta } from '../../modules/recepcion/recepcion.meta'

const Recepcion = () => <SchemeView metaModel={RecepcionMeta} />

export default Recepcion
